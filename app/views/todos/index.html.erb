<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body class="well">
    <table class="table table-hover" style="margin-top:50px;">
      <thead>
        <th>Descripcion</th>
        <th>Completado</th>
        <th colspan="3"></th>
      </thead>

      <tbody>
        <% @todos.each do |todo| %>
          <tr class="table-active">
            <td><%= todo.description%></td>
            <td><%= todo.completed %></td>
            <td><%= link_to "Show", todo %></td>
            <td><%= link_to "Edit", edit_path(todo) %></td>
            <td><%= link_to "Borrar", todo, method: :delete, data: { confirm: 'Are you sure?' }%></td>
            <td><%= link_to "Completed", complete_path(todo),class:"btn btn-warning" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

  </body>
</html>
